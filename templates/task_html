<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Tasks – JobDash</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
        sans-serif;
      background: linear-gradient(to bottom right, #f8fafc, #eef2f7);
      color: #0f172a;
      line-height: 1.5;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    .container {
      max-width: 1100px;
      margin: 0 auto;
      padding: 0 16px;
    }

    /* HEADER (même que index) */

    header {
      background: transparent;
    }

    .nav {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 14px 0;
    }

    .brand {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .logo-circle {
      width: 36px;
      height: 36px;
      border-radius: 999px;
      background: #2563eb;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 700;
      font-size: 16px;
      color: #ffffff;
    }

    .brand-text-title {
      font-weight: 700;
      font-size: 18px;
    }

    .brand-text-sub {
      font-size: 12px;
      color: #6b7280;
    }

    .nav-links {
      display: flex;
      gap: 22px;
      font-size: 14px;
      color: #4b5563;
      align-items: center;
    }

    .nav-links a:hover {
      color: #2563eb;
    }

    .nav-cta {
      padding: 7px 16px;
      border-radius: 999px;
      background: #2563eb;
      color: #ffffff;
      font-size: 13px;
      font-weight: 600;
    }

    /* MAIN */

    .page-wrap {
      padding: 40px 16px 50px;
    }

    .tasks-layout {
      background: #ffffff;
      border-radius: 24px;
      border: 1px solid #e5e7eb;
      box-shadow: 0 8px 24px rgba(15, 23, 42, 0.08);
      padding: 26px 24px 28px;
      display: grid;
      grid-template-columns: 3fr 1.2fr;
      gap: 28px;
    }

    .tasks-header {
      margin-bottom: 18px;
    }

    .tasks-title {
      font-size: 24px;
      font-weight: 700;
      margin-bottom: 4px;
    }

    .tasks-subtitle {
      font-size: 14px;
      color: #6b7280;
    }

    /* Filter bar */

    .filter-bar {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-bottom: 18px;
    }

    .filter-pill {
      padding: 6px 12px;
      border-radius: 999px;
      border: 1px solid #e5e7eb;
      background: #f9fafb;
      font-size: 12px;
      color: #4b5563;
      cursor: pointer;
    }

    .filter-pill.primary {
      background: #2563eb;
      color: #ffffff;
      border-color: #2563eb;
      font-weight: 600;
    }

    /* Task list */

    .task-list {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .task-card {
      display: flex;
      justify-content: space-between;
      gap: 14px;
      padding: 14px 16px;
      border-radius: 14px;
      border: 1px solid #e5e7eb;
      background: #f9fafb;
      transition: 0.2s ease;
    }

    .task-card:hover {
      background: #ffffff;
      box-shadow: 0 6px 16px rgba(15, 23, 42, 0.12);
      transform: translateY(-2px);
    }

    .task-main {
      flex: 1;
    }

    .task-title {
      font-size: 15px;
      font-weight: 600;
      margin-bottom: 4px;
    }

    .task-meta {
      font-size: 12px;
      color: #6b7280;
      margin-bottom: 4px;
    }

    .task-desc {
      font-size: 12px;
      color: #4b5563;
    }

    .task-right {
      display: flex;
      flex-direction: column;
      align-items: flex-end;
      justify-content: space-between;
      gap: 6px;
      min-width: 90px;
    }

    .task-pay {
      background: #d1fae5;
      color: #065f46;
      padding: 6px 12px;
      border-radius: 999px;
      font-size: 13px;
      font-weight: 700;
    }

    .task-cta {
      font-size: 12px;
      color: #2563eb;
    }

    /* Empty state */

    .empty-state {
      border-radius: 18px;
      border: 1px dashed #d1d5db;
      background: #f9fafb;
      padding: 24px 20px;
      text-align: left;
      font-size: 14px;
      color: #4b5563;
    }

    .empty-title {
      font-weight: 600;
      font-size: 16px;
      margin-bottom: 6px;
    }

    .empty-text {
      margin-bottom: 14px;
    }

    .btn-outline-main {
      display: inline-block;
      padding: 9px 16px;
      border-radius: 999px;
      border: 1px solid #2563eb;
      color: #2563eb;
      font-size: 13px;
      font-weight: 600;
      background: #ffffff;
    }

    /* Sidebar */

    .tasks-sidebar-title {
      font-size: 14px;
      font-weight: 600;
      margin-bottom: 6px;
    }

    .tasks-sidebar-text {
      font-size: 13px;
      color: #6b7280;
      margin-bottom: 16px;
    }

    .sidebar-card {
      border-radius: 16px;
      border: 1px solid #e5e7eb;
      background: #f9fafb;
      padding: 14px 14px 16px;
      font-size: 12px;
      color: #4b5563;
      margin-bottom: 12px;
    }

    .sidebar-card strong {
      color: #111827;
    }

    footer {
      padding: 20px 16px 30px;
      font-size: 12px;
      color: #9ca3af;
      text-align: center;
    }

    footer a {
      color: #9ca3af;
    }

    @media (max-width: 880px) {
      .tasks-layout {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <!-- HEADER -->
  <header>
    <div class="container nav">
      <div class="brand">
        <div class="logo-circle">JD</div>
        <div>
          <div class="brand-text-title">JobDash</div>
          <div class="brand-text-sub">Jobcenter online · quick cash jobs</div>
        </div>
      </div>

      <nav class="nav-links">
        <a href="index.html">Home</a>
        <a href="tasks.html">Tasks</a>
        <a href="post_job.html">Post a job</a>
        <a href="about.html">About</a>
        <a href="contact.html">Contact</a>
        <a class="nav-cta" href="post_job.html">Post a job</a>
      </nav>
    </div>
  </header>

  <!-- MAIN -->
  <main>
    <section class="page-wrap">
      <div class="container">
        <div class="tasks-layout">
          <!-- COLONNE GAUCHE -->
          <div>
            <div class="tasks-header">
              <h1 class="tasks-title">All available tasks</h1>
              <p class="tasks-subtitle">
                Browse the latest local jobs posted on JobDash.
              </p>
            </div>

            <div class="filter-bar">
              <span class="filter-pill primary">All tasks</span>
              <span class="filter-pill">Cleaning</span>
              <span class="filter-pill">Moving</span>
              <span class="filter-pill">Yard work</span>
              <span class="filter-pill">Shopping & delivery</span>
            </div>

            <!-- LISTE DES TASKS -->
            <div class="task-list">
              {% if tasks %}
                {% for task in tasks %}
                  <a href="/tasks/{{ task.id }}" class="task-card">
                    <div class="task-main">
                      <div class="task-title">{{ task.title }}</div>
                      <div class="task-meta">
                        {{ task.location }} · {{ task.date }} · {{ task.duration }}
                      </div>
                      <div class="task-desc">
                        {{ task.description }}
                      </div>
                    </div>
                    <div class="task-right">
                      <div class="task-pay">
                        {{ task.pay }}
                      </div>
                      <div class="task-cta">View details →</div>
                    </div>
                  </a>
                {% endfor %}
              {% else %}
                <div class="empty-state">
                  <div class="empty-title">No tasks yet.</div>
                  <div class="empty-text">
                    Be the first to post a job on JobDash. Create a quick task
                    for cleaning, moving, yard work, shopping or more – workers
                    in your area will see it immediately.
                  </div>
                  <a href="post_job.html" class="btn-outline-main">
                    Post a job
                  </a>
                </div>
              {% endif %}
            </div>
          </div>

          <!-- COLONNE DROITE (SIDEBAR) -->
          <aside>
            <div class="tasks-sidebar-title">Why post on JobDash?</div>
            <div class="tasks-sidebar-text">
              Get help fast for same-day or next-day tasks, without long forms
              or interviews.
            </div>

            <div class="sidebar-card">
              <strong>Same-day workers</strong><br />
              Many JobDash workers are available today or tomorrow for urgent
              jobs.
            </div>

            <div class="sidebar-card">
              <strong>Simple pricing</strong><br />
              Set your budget, describe the task, and workers choose whether to
              accept.
            </div>

            <div class="sidebar-card">
              <strong>Local only</strong><br />
              JobDash focuses on local, in-person tasks so you get help that
              actually shows up.
            </div>
          </aside>
        </div>
      </div>
    </section>
  </main>

  <!-- FOOTER -->
  <footer>
    © 2025 JobDash Online. All rights reserved ·
    <a href="privacy.html">Privacy Policy</a> ·
    <a href="terms.html">Terms of Use</a>
  </footer>
</body>
</html>