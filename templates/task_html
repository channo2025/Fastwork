<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Tasks – JobDash</title>
    <link rel="stylesheet" href="/static/styles.css">
</head>
<body>

<header class="header">
    <div class="brand">
        <a href="/">JD</a>
        <span class="brand-text">JobDash</span>
    </div>

    <nav class="nav">
        <a href="/#how">How it works</a>
        <a href="/tasks" class="active">Tasks</a>
        <a href="/post-job">Post a job</a>
        <a href="/#faq">FAQ</a>
    </nav>
</header>

<section class="section">
    <h1>All tasks</h1>
    <p>Here are all the jobs available on JobDash.</p>

    {% if tasks %}
        {% for t in tasks %}
            <div class="task-card">
                <h3>{{ t.title }}</h3>
                <p>{{ t.description }}</p>
                <p><strong>{{ t.location }}</strong> – {{ t.when }}</p>
                <p class="tag">{{ t.task_type }}</p>
                <p class="budget">${{ t.budget }}</p>

                <a class="btn-primary" href="/task/{{ loop.index }}">View details</a>
            </div>
        {% endfor %}
    {% else %}
        <p>No tasks available yet.</p>
    {% endif %}
</section>

</body>
</html>