<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Tasks – JobDash</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <style>
        body {
            margin: 0;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            background: #f5f5f5;
            color: #111827;
        }

        .page {
            max-width: 1100px;
            margin: 0 auto;
            padding: 24px 16px 60px;
        }

        /* HEADER (même style que la home) */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 24px;
        }

        .brand {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .badge-logo {
            width: 40px;
            height: 40px;
            border-radius: 999px;
            background: #2563eb;
            color: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
        }

        .brand-text-title {
            font-weight: 700;
        }

        .brand-text-sub {
            font-size: 0.8rem;
            color: #6b7280;
        }

        .nav {
            display: flex;
            gap: 18px;
            font-size: 0.9rem;
        }

        .nav a {
            text-decoration: none;
            color: #4b5563;
        }

        .nav a:hover {
            text-decoration: underline;
        }

        /* TITLE */
        .page-title {
            font-size: 1.7rem;
            font-weight: 700;
            margin: 8px 0 4px;
        }

        .page-sub {
            font-size: 0.95rem;
            color: #6b7280;
            margin-bottom: 20px;
        }

        /* FILTERS */
        .filters {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin-bottom: 22px;
        }

        .filter-btn {
            padding: 8px 16px;
            border-radius: 999px;
            border: 1px solid #e5e7eb;
            background: white;
            font-size: 0.85rem;
            cursor: pointer;
            text-decoration: none;
            color: #374151;
        }

        .filter-btn.active {
            background: #2563eb;
            color: white;
            border-color: #2563eb;
        }

        /* TASKS LIST */
        .tasks-list {
            display: flex;
            flex-direction: column;
            gap: 14px;
        }

        .task-card {
            background: white;
            border-radius: 16px;
            padding: 16px 18px;
            box-shadow: 0 4px 12px rgba(15, 23, 42, 0.08);
            display: flex;
            flex-direction: column;
            gap: 6px;
        }

        .task-header {
            display: flex;
            justify-content: space-between;
            align-items: baseline;
        }

        .task-title {
            font-weight: 600;
        }

        .task-budget {
            font-weight: 600;
            color: #16a34a;
        }

        .task-meta {
            font-size: 0.85rem;
            color: #6b7280;
        }

        .task-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
            margin-top: 6px;
        }

        .tag {
            font-size: 0.78rem;
            padding: 4px 10px;
            border-radius: 999px;
            background: #eff6ff;
            color: #1d4ed8;
        }

        .empty-text {
            font-size: 0.9rem;
            color: #6b7280;
            font-style: italic;
        }

        @media (max-width: 700px) {
            .header {
                flex-direction: column;
                align-items: flex-start;
                gap: 8px;
            }

            .nav {
                font-size: 0.85rem;
            }
        }
    </style>
</head>
<body>
<div class="page">

    <!-- HEADER -->
    <header class="header">
        <div class="brand">
            <div class="badge-logo">JD</div>
            <div>
                <div class="brand-text-title">JobDash</div>
                <div class="brand-text-sub">Jobcenter online</div>
            </div>
        </div>

        <nav class="nav">
            <a href="/">Home</a>
            <a href="/tasks">Tasks</a>
            <a href="/post-job">Post a job</a>
            <a href="/#faq">FAQ</a>
        </nav>
    </header>

    <!-- TITLE -->
    <h1 class="page-title">All tasks</h1>
    <p class="page-sub">
        Browse all the jobs currently available on JobDash. Use filters to see only the type of work you want.
    </p>

    <!-- FILTERS -->
    <div class="filters">
        <a href="/tasks"
           class="filter-btn {% if not selected_type %}active{% endif %}">
            All tasks
        </a>

        {% for t in task_types %}
            <a href="/tasks?task_type={{ t }}"
               class="filter-btn {% if selected_type == t %}active{% endif %}">
                {{ t }}
            </a>
        {% endfor %}
    </div>

    <!-- TASKS LIST -->
    {% if jobs and jobs|length > 0 %}
        <div class="tasks-list">
            {% for job in jobs %}
                <article class="task-card">
                    <div class="task-header">
                        <div class="task-title">{{ job.title }}</div>
                        {% if job.budget %}
                            <div class="task-budget">${{ job.budget }}</div>
                        {% endif %}
                    </div>

                    {% if job.description %}
                        <div class="task-meta">{{ job.description }}</div>
                    {% endif %}

                    <div class="task-meta">
                        {% if job.location %}{{ job.location }}{% else %}Location not specified{% endif %}
                        {% if job.when %} · {{ job.when }}{% endif %}
                    </div>

                    <div class="task-tags">
                        {% if job.task_type %}
                            <span class="tag">{{ job.task_type }}</span>
                        {% endif %}
                        <span class="tag">Posted just now</span>
                    </div>
                </article>
            {% endfor %}
        </div>
    {% else %}
        <p class="empty-text">
            No tasks yet. Be the first one to <a href="/post-job">post a job</a> on JobDash.
        </p>
    {% endif %}
</div>
</body>
</html>